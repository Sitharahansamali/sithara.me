---
import profileImage from '../assets/sithara.png';

const name = "Sithara Hansamali";
const title = "AI Enthusiast | Computer Science Undergraduate";
const bio = "Passionate about Artificial Intelligence, data-driven solutions, and innovative technologies. I enjoy learning machine learning concepts, building smart systems, and exploring how AI can solve real-world challenges.";
---

<section class="hero" id="hero">
	<div class="container">
		<div class="hero-grid">
			<div class="hero-content">
				<p class="hero-kicker">HELLO!</p>
				<h1 class="hero-title">
					I Am <span class="highlight">{name}</span>
				</h1>
				<h2 class="hero-subtitle">{title}</h2>
				<p class="hero-bio">{bio}</p>
				<div class="hero-cta">
					<a href="/about" class="btn btn-primary">About Me</a>
					<a href="/contact" class="btn btn-secondary">Connect With Me</a>
				</div>
			</div>

			<div class="hero-image-container">
				<div class="image-wrapper">
					<div class="image-backdrop"></div>
					<img src={profileImage.src} alt={name} class="profile-image" />
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.hero {
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 8rem 2rem 4rem;
		background: transparent;
		color: white;
		overflow: hidden;
		position: relative;
	}

	.hero::before {
		content: '';
		position: absolute;
		inset: 0;
		background:
			radial-gradient(circle at 16% 16%, rgba(255, 99, 99, 0.12), transparent 18%),
			radial-gradient(circle at 80% 18%, rgba(255, 99, 99, 0.08), transparent 20%),
			radial-gradient(circle at 84% 58%, rgba(255, 99, 99, 0.07), transparent 22%);
		pointer-events: none;
	}

	.container {
		max-width: 1200px;
		width: 100%;
		margin: 0 auto;
		position: relative;
		z-index: 1;
	}

	.hero-grid {
		display: grid;
		grid-template-columns: 1.05fr 0.95fr;
		gap: 2rem;
		align-items: center;
		animation: fadeInUp 1s ease-out;
	}

	.hero-content {
		text-align: left;
		max-width: 620px;
	}

	.hero-kicker {
		margin: 0 0 1rem;
		font-size: 1rem;
		font-weight: 800;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		color: #ff6b6b;
	}

	.hero-title {
		font-size: clamp(2.8rem, 6vw, 4.2rem);
		font-weight: 800;
		margin-bottom: 1rem;
		line-height: 1.1;
		max-width: 12ch;
	}

	.highlight {
		color: #ffffff;
		text-shadow: 0 2px 14px rgba(0, 0, 0, 0.35);
	}

	.hero-subtitle {
		font-size: clamp(1.2rem, 2.6vw, 1.85rem);
		font-weight: 400;
		margin-bottom: 1.5rem;
		color: rgba(255, 255, 255, 0.9);
	}

	.hero-bio {
		font-size: 1.08rem;
		line-height: 1.8;
		max-width: 620px;
		margin: 0 0 2.4rem;
		color: rgba(255, 255, 255, 0.85);
	}

	.hero-cta {
		display: flex;
		gap: 1rem;
		justify-content: flex-start;
		flex-wrap: wrap;
	}

	.btn {
		padding: 1rem 2rem;
		border-radius: 50px;
		text-decoration: none;
		font-weight: 700;
		font-size: 1rem;
		transition: all 0.3s ease;
		display: inline-flex;
		align-items: center;
		justify-content: center;
	}

	.btn-primary {
		background: #ff5a57;
		color: #ffffff;
		box-shadow: 0 12px 24px rgba(255, 90, 87, 0.25);
	}

	.btn-primary:hover {
		background: #ff6e6b;
		transform: translateY(-2px);
	}

	.btn-secondary {
		background: rgba(255, 255, 255, 0.06);
		color: #ffffff;
		border: 1px solid rgba(255, 255, 255, 0.28);
	}

	.btn-secondary:hover {
		background: rgba(255, 255, 255, 0.12);
		transform: translateY(-2px);
	}

	.hero-image-container {
		display: flex;
		justify-content: flex-end;
		align-items: center;
		align-self: end;
	}

	.image-wrapper {
		position: relative;
		width: min(100%, 560px);
		height: clamp(540px, 72vh, 760px);
		border-radius: 0;
		overflow: visible;
		border: none;
		box-shadow: none;
		background: transparent;
		animation: float 4s ease-in-out infinite;
	}

	.image-backdrop {
		position: absolute;
		inset: 22px 10px 0 18px;
		width: 100%;
		height: 100%;
		border-radius: 0;
		background:
			radial-gradient(circle at 30% 20%, rgba(255, 112, 112, 0.25), transparent 22%),
			radial-gradient(circle at 82% 26%, rgba(255, 112, 112, 0.14), transparent 18%),
			linear-gradient(180deg, rgba(255, 255, 255, 0.04), rgba(255, 255, 255, 0));
		transform: rotate(-1deg);
		pointer-events: none;
	}

	.profile-image {
		position: relative;
		z-index: 1;
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: center top;
		transition: transform 0.35s ease;
	}

	.image-wrapper:hover .profile-image {
		transform: scale(1.03);
	}

	@keyframes fadeInUp {
		from {
			opacity: 0;
			transform: translateY(30px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes float {
		0%, 100% {
			transform: translateY(0);
		}
		50% {
			transform: translateY(-12px);
		}
	}

	@media (max-width: 968px) {
		.hero {
			padding-top: 7rem;
		}

		.hero-grid {
			grid-template-columns: 1fr;
			gap: 2.5rem;
			text-align: center;
		}

		.hero-content {
			text-align: center;
			max-width: none;
		}

		.hero-cta {
			justify-content: center;
		}

		.hero-image-container {
			justify-content: center;
		}

		.image-wrapper {
			width: min(100%, 360px);
			height: 440px;
		}

		.hero-title {
			max-width: none;
		}
	}

	@media (max-width: 560px) {
		.hero {
			padding-inline: 1rem;
		}

		.hero-title {
			font-size: 2.35rem;
		}

		.image-wrapper {
			width: min(100%, 290px);
			height: 360px;
		}

		.hero-bio {
			font-size: 1rem;
		}

		.btn {
			width: 100%;
		}
	}
</style>
